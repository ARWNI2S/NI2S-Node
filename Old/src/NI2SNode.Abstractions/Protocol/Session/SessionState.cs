namespace NI2S.Node.Protocol.Session
{
    //TODO: Session to connection??
    public enum SessionState
    {
        None = 0,
        Initialized = 1,
        Connected = 2,
        Closed = 3
    }
}