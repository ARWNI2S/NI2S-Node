// This file is used by Code Analysis to maintain SuppressMessage
// attributes that are applied to this project.
// Project-level suppressions either have no target or are given
// a specific target and scoped to a namespace, type, member, etc.

using Orleans;

namespace ARWNI2S.Engine.Core
{
    public interface INiisGrain : IGrainWithGuidKey
    {
        //Task<IActorState> GetStateAsync();
        //Task UpdatePropertyAsync(string propertyName, object value);
        //Task UpdatePropertiesAsync(Dictionary<string, object> updates);
        //Task UpdateFullStateAsync(IActorState newState);
        //Task PersistStateAsync();
        //Task SubscribeClientAsync(IActorClient client);
        //Task NotifyClientsAsync(string eventName, object eventData);
    }

}
